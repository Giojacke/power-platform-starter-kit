# scripts/powerbi

Fabric/PBIP wrapper scripts for the Power BI module. Planned scripts:

- **PBIP scaffold** — generate the `<Report>.Report/` and
  `<Report>.SemanticModel/` folder structure
- **Fabric Git sync helpers** — wrap the Fabric Git Integration workflow
  this module relies on instead of `pac` — see
  [ADR-0001](../../docs/adr/ADR-0001-estructura-modular-power-platform.md)

Currently empty — scripts will be added incrementally.
